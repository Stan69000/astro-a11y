# static-site example

This is a tiny static fixture used to validate the scanner against real built HTML files.

It intentionally contains:
- an image without alt text
- a button without accessible name
- low-contrast text
- an iframe without title
